# n8n pipeline: Scan triggers workflow → results page shows workflow output
# Option A: Webhook (Medical Agent Backend) - works on free trial, expects multipart image
N8N_WEBHOOK_URL=https://your-instance.app.n8n.cloud/webhook/scan

# Option B: Workflow-run API (paid plan)
N8N_BASE_URL=http://localhost:5678
N8N_API_KEY=
N8N_WORKFLOW_ID_SCAN=

# ElevenLabs (optional): Used in n8n workflow for TTS. Get API key and Rachel voice_id from Voice Lab.
# ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM

# Discord (optional): Webhook URL for Guardian FAIL alerts. Set in n8n Discord Webhook node.
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_ID/YOUR_TOKEN
