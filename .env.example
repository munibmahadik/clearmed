# n8n pipeline: Scan triggers workflow → results page shows workflow output
# Option A: Webhook (Medical Agent Backend) - works on free trial, expects multipart image
N8N_WEBHOOK_URL=https://axlegeek.app.n8n.cloud/webhook/scan

# Option B: Workflow-run API (paid plan)
N8N_BASE_URL=http://localhost:5678
N8N_API_KEY=
N8N_WORKFLOW_ID_SCAN=
